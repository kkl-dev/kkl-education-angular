import { LabelLengthDirective } from './label-length.directive';

describe('LabelLengthDirective', () => {
  it('should create an instance', () => {
    const directive = new LabelLengthDirective();
    expect(directive).toBeTruthy();
  });
});
